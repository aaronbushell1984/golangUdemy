package userdefinedtype

import "testing"

func TestPrintTypeInt(t *testing.T) {
// 	want := "My type is: " + mytype + " and is of underlying type: " + underlyingtype
// 	if got := PrintType(typename); got != want {
// 		t.Errorf("CreateType(mytype, int) = %q, want %q", got, want)
// 	}
}